export const colors = {
  theme: "#ff9100",
  primary: "#eee",
  secondary: "#eee",
};

export const sizes = {
  hourHeight: 120,
  slotSize: parseInt(process.env.REACT_APP_SLOT_SIZE || "5") || 5,
};
